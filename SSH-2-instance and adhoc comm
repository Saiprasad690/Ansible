Create two instances and only in one ec2 update and install ansible
ssh-keygen in 1st ec2 cd to the path of id_rsa.pub and copy the key into the 2nd ec2 vim ~./ssh/authorized_keys. replace the keys of id_rsa.pub in the 2nd ec2 vim ~./ssh/authorized_keys.
# ssh (private ip of 2nd ec2) and you will login to the 2nd but now look at the ip and exit

# create an inventory file and give 2nd ec2 ip init

## ansible adhoc comment##
ansible -i inventory all -m "shell" -a "df"

